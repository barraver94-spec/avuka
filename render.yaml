services:
  - type: web
    name: avuka-pdf-renderer
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --timeout 120 --workers 1 --max-requests 200 --max-requests-jitter 30
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
